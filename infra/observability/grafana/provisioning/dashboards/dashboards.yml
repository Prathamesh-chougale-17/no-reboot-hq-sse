apiVersion: 1

providers:
  - name: no-reboot-hq
    orgId: 1
    folder: No Reboot HQ
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
